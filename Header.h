#pragma once

namespace cPlusPlus {
	enum objects {
		aClass,
		aFunction,
		aDefinition,
		aNamespace,
		anEnum,
		anAbstractClass
	};
}

// Presented as if they are in the cPlusPlus namespace
enum otherObjects {
	otherClasses,
	otherFunctions,
	otherDefinitions,
	otherNamespaces,
	otherEnums,
	otherAbstractClasses
};
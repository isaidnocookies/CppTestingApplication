#pragma once

namespace cPlusPlus {
	enum objects {
		aClass,
		aFunction,
		aDefinition,
		aNamespace,
		anEnum,
		anAbstractClass
	};
}

enum otherObjects {
	otherClasses,
	otherFunctions,
	otherDefinitions,
	otherNamespaces,
	otherEnums,
	otherAbstractClasses
};